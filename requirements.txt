# requirements.txt — generated by assistant
# Adjust versions as needed for your platform/environment.
# Note: ScaNN may require a platform-specific wheel; install `scann` separately if needed.

numpy==2.3.4
pandas==2.3.3
torch==2.9.0
transformers==4.57.1
sentence-transformers>=2.2.2
scann
datasets>=2.13.0
h5py>=3.8.0
fastapi==0.123.0
uvicorn>=0.22.0
jupyterlab>=4.0.0

# Utilities
tqdm>=4.65.0
httpx>=0.24.0

# Optional: add these if you plan to run rear-end/frontend integration
python-multipart

# End of file
